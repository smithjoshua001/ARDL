#pragma once
#include <ostream>

#include "ARDL/Util/Math.hpp"

// namespace ARDL {
//     using namespace Util::Math;
//     namespace Math {
//         template <typename Exp> class LieExpression {
//         public:
//             template <typename Exp2> inline void operator()(LieExpression<Exp2> &out) const {
//                 static_cast<Exp const &>(*this)(out);
//             }

//             template <typename Derived> inline void operator()(const Eigen::MatrixBase<Derived> &out) const {
//                 static_cast<Exp const &>(*this)(out);
//             }
//         }
//     }
// }
